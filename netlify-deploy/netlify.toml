# Alleen deze map deployen (drag & drop of publish dir = netlify-deploy).
# Geen build, geen asset-optimalisatie — spaart tijd en credits.
[build]
  publish = "."
  command = "true"

[build.processing]
  skip_processing = true

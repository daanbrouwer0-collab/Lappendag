# Let op: de echte Netlify-deploy map is `netlify-deploy/`
# (alleen loader, geen build, spaart free credits).
# Deze root-toml voorkomt per ongeluk de hele repo + Lap-set te publishen.
[build]
  command = "mkdir -p publish && cp netlify-index.html publish/index.html"
  publish = "publish"
